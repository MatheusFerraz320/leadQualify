# Neon (PostgreSQL serverless) — preencha com a URL do seu projeto
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Chave secreta para assinatura dos JWT (gerar com: openssl rand -hex 32)
JWT_SECRET=""

# Tempo de expiração do token de acesso
JWT_EXPIRES_IN="7d"
